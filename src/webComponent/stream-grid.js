// Registers both custom elements and re-exports the StreamGrid class.
// Import this file once (e.g. in a <script type="module"> tag) and
// <stream-grid> / <stream-grid-column> become available globally.

export { StreamGridElement } from './StreamGridElement.js';
export { StreamGridColumn } from './StreamGridColumn.js';
export { StreamGrid } from '../StreamGrid.js';
